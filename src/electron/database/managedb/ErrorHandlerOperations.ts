export const ErrorFn=async (error:string) => {
  console.error("❌ Error:========ERROR HANDLE OPERATION", error);
  // Handle the error as needed, e.g., log it to a file or send it to a server
  // For now, just log it to the console
}